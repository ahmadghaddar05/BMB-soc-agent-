# Changelog

This repository has 130 commits and no version tags or release branches. The entries below reconstruct notable implementation changes from Git commit dates and subjects; they are not formal semantic-version releases. Current limitations remain governed by [Feature Status](FEATURE_STATUS.md) and [Known Issues](KNOWN_ISSUES.md), not by historical commit wording.

## 2026-08-19 — MITRE Coverage

- Added MITRE tactic/technique metadata and incident-path generation (`ded4925`).
- Added the evidence-backed incident and aggregate coverage workspace (`10978c7`).
- Corrected mapping and incident coverage-pipeline behavior (`511258d`).

## 2026-08-18 — Digital Twin and Replay

- Added the Digital Twin and Attack Simulator workspaces (`1c62b59`).
- Added animated replay of persisted alert decisions (`c2e959d`).
- Expanded replay into detailed AI operation/evidence phases (`27c0414`).

## 2026-08-10 to 2026-08-11 — Connectors and Analyst Workspace Redesign

- Completed the Splunk adapter and secure dashboard-managed connector storage (`17cece6`, `a24ad6b`).
- Added Splunk normalization, repeated-alert grouping, triggered-result ingest, and index scoping (`5010116`, `cb3ec16`, `0dec346`, `eeb98c6`).
- Introduced the later BMB product-token layer and redesigned Live Monitoring, Security Analytics, alert triage, cases, incidents, investigations, assets, and entity intelligence (`4fa80be` through `1ca7c92`).
- Fixed hard-refresh routing for the `/assets` React route (`dc4067a`).

## 2026-07-28 to 2026-07-31 — Identity, Model Routing, Provenance, and Decision Review

- Added database-managed RBAC and unified login (`7803f13`).
- Added Hermes model switching/provenance and fail-closed route verification (`dca9432`, `f39f7fb`, `fa6d686`).
- Added workflow provenance and explainable alert-triage records (`9e8f42b`, `d36e2d9`).
- Added protected severity-aware retention and fixed PostgreSQL parameter typing (`b2528fa`, `de53c3d`, `b315340`).
- Added explainable incident correlation, durable analyst review, and decision-assurance metrics (`b1a7652`, `73a5e50`, `9eddd9a`).
- Added SOC analytics and entity relationship visualization (`b52753f`).
- Added the first Splunk connector commit (`cb2a039`).
- A commit dated 2026-07-23 added `LLM_PROVIDER`/`NOUS_*` to Compose (`9ef10fd`); those values are not consumed by current API code. See [KI-022](KNOWN_ISSUES.md#ki-022-compose-advertises-ai-provider-variables-the-api-does-not-read).

## 2026-07-20 to 2026-07-27 — Role-Specific Product Areas

- Added role-aware frontend foundations and executive/analyst/administrative page groups (`ea358ac`, `62272a4`).
- Reworked the executive dashboard around database-derived security metrics (`60478b7`).
- Added server-enforced role authentication (`e6fa9c2`).
- Redesigned incident selection/command and safe response-simulation workspaces (`7a4eff2`, `36e20d9`).
- Scoped AI chat conversations by role and exposed Hermes diagnostic failures (`4913aba`, `ecede3a`).

## 2026-07-15 to 2026-07-17 — Hermes and Durable Workflows

- Added the grounded Hermes SOC assistant and routed chat through it when configured (`004e0d0`, `7620865`).
- Added Hermes toolset/capability handling and migrated alert triage and incident correlation (`ad377d7`, `f55b9de`, `c0d622c`).
- Added durable investigations and cases (`101f1d8`).
- Added controlled action requests/approvals, autonomous orchestration records, and simulated response workflows (`ec689be`, `96ff78c`, `46fa03a`).
- Added coordinated deterministic telemetry scenarios (`46170b7`).

## 2026-07-14 — Initial Platform and UI Modules

- Created the initial repository (`e4145c5`).
- Added the application shell, security overview, responsive styling, global search, AI assistant entry point, and information tooltips.
- Added the first Alerts, Incidents, AI Triage, Threat Intelligence, Assets, Vulnerabilities, Investigations, Playbooks, Integrations, and Cases page implementations.
- Added grouped-alert time filters and successive grouped-query SQL corrections.
- Added and refined the BMB PNG branding and favicon assets.

## Release Process Status

| Item | Current state |
|---|---|
| Semantic version tags | None. |
| GitHub/GitLab release objects | No release metadata found in the local Git history. |
| Automated changelog generation | Not configured. |
| CI/CD release workflow | Not configured. |
| Recommended next step | Adopt version tags and require changelog entries for client-visible or schema-changing work. |

---

Last updated: 2026-08-26 — generated by complete local Git-history review reconciled with the current code audit
